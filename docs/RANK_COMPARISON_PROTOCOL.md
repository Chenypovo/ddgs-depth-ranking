# Ordinal supervision comparison, 2026-09-13

User said continue after reviewing CANDIDATE.md plan. Same existing GPU SSH <port> authorised, leave instance ON after completion. Estimate25–45min, few GB, no new download. Runner hard cap2700seconds per formal cohort; no shutdown action and no automatic retry.

Hypothesis: augmentation-consistent pair selection reduces incorrect ordinal supervision and improves novel-view LPIPS beyond a matched-count ordinary ordinal loss. Previous training-only diagnostics support ranking reliability, not rendering quality. Candidate novelty unconfirmed; SparseNeRF, In Depth We Trust and UGOT are relevant prior art.

Fixed arms: off, ordinary, consensus; seeds0/1/2; all10000steps, final-checkpoint only. Same LLFF trex3train/7development images,504x378, old training-only initial PLY and reconstructed far masks. trex is DEVELOPMENT, not independent test. Preserve original code and completed results; isolated DDGS_rank source. Upstream depth/density behaviour preserved in all arms, no previous correction mixed in. Same source/evaluator/hardware for all nine runs.

Module settings exactly from candidate: teacher caches original518/flip518/original392; min teacher standardised gap.05; pair radius8..64px; candidate pool16384; maximum4096pairs; all3ordinal signs agree for C; ordinary samples same N from same ordered random pool. Local private torch generator seeded1000003*model_seed+iteration+7919, global RNG unaffected. Pair-selection uses only teacher data. Hinge margin.05, rendered inverse-depth scale detached IQR, loss weight0 until1000 then linear to.01 at3000. No sweeps. Telemetry records actual selected, valid and violated pairs every active step. Invalid rendered alpha<=1e-4/depth numerator<=1e-6 pairs contribute zero and are logged; reject supervision-matching interpretation if active counts materially differ.

Packed differentiable depth render uses [normalised camera-z,1,0] colour channels, no stochastic masking on the depth pass. This adds one renderer forward/backward for B/C; no new trainable parameters. Original RGB dropout pass supplies densification screen-space gradients; depth loss updates Gaussian parameters through its own graph. Log all implementation details rather than silently changing densification rules.

Integration: source syntax/import, matching counts and consensus signs on real caches, global RNG unchanged, ordinal loss/gradient direction, packed depth equals separate numerator/alpha renders, finite nonzero gradients to xyz/scale/opacity, checkpoint RGB render unchanged after backward without optimiser step. Then a1200step consensus integration pilot (no quality-based selection; no independent test evaluation). Formal cohort starts only after pilot completion.

Primary metric standard VGG LPIPS[-1,1] at original opacity. PSNR/SSIM secondary, training time and peak allocated/reserved memory reported. Both original opacity and fixedq=.85 evaluated for EVERY arm. Evaluator math preserved in independent script, output directories isolated. Three-seed means and paired results, no best seed/checkpoint substitution. C must beat B as well as A: >1% relative LPIPS mean reduction, consistent seed direction and no material PSNR/SSIM regression before further investment. Secondary edge MAE from saved PNGs is a quantised visual-error diagnostic, not a replacement primary. No claim of cross-scene validation.
