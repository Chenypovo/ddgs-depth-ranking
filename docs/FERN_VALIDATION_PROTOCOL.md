# Frozen-method fern validation — 2026-09-13

User authorised continuing D²GS + ordinary ordinal supervision on one new scene; existing SSH <port> GPU, leave instance ON. Selected LLFF fern before evaluating outputs, to examine plant leaves and boundaries in a different scene. Existing archive, no new download. Estimate30–50min including preprocessing and6fullruns;1–3GB additional artifacts. Preprocessing timeout1200sec, formal cohort cap2700sec; no automatic retries or parameter tuning.

Same source/data adaptations as trex.3train images selected uniformly from non-eighth frames,3evaluation images from20total, r8. Training-only3view triangulation/stereo with existing prepare_scene.py. No fallback to full-view points. LLFF supplied camera poses are reused. Frozen DAV2Small: original518/flip518unflipped/original392, robust median/IQR normalisation, original-depth farthest5percent masks. Source DDGS_fern copied from DDGS_rank; only remove trex-specific shape assertion, no loss/sampling changes. Original baseline still retains upstream depth/density dropout behaviour.

Arms off vs ordinary, seeds0/1/2, all10000steps finalcheckpoint. Same hyperparameters as trex: depth_weight.15,density_weight.85,drop_min.05,drop_max.5,mask_param5,lambda_far.5. Rank margin.05, weight0 until1000 then linear to.01 at3000, radius8..64,pool16384,max4096pairs. Preserve existing ordinary sampling exactly, including count=min4096,number of augmentation-consistent eligible pairs; no consensus arm. This intentionally keeps the prior tested ordinary method unchanged. Private per-step RNG unchanged. No choice based on validation scores.

Integration: real3train scene/camera/densepoint identity, priors/render shapes, finite positive loss and nonzero finite xyz/scale/opacity gradients, global CUDA RNG unaffected. Then1200step ordinary pilot for integration, no quality selection. Formal only after passed checks and complete pilot.

Evaluate PSNR,SSIM,VGG LPIPS standard[-1,1] (primary), preserve repo[0,1] separately. Full3views, all3seeds, final10000checkpoint. q1 main; fixed opacity multiplier.85 secondary for every model. Both modes save10sec24fps static camera-path videos and paired RGB images. Training time and peak allocated/reserved memory included. Log selected/active/violated ordinal pairs and report invalid exposure. Predeclared desirable result: >1percent mean LPIPS decrease,3/3seed direction, no material secondary quality regression. Report every outcome; do not change objective to favour results. Quantised PNG GTtop20percentSobel MAE only as secondary diagnostic, never geometrical GT.

This is one additional scene with settings selected using trex. Report separately, and equal-weight scene averages only as exploratory summaries, not full benchmark or universal significance. Generic depth ranking is prior work (SparseNeRF and related depth-supervised reconstruction); contribution is diagnosis/adaptation/evaluation, not invention of ranking. Consensus candidate remains paused. Final checkpoint/source/cache/split hashes and full backup required. No instance shutdown or additional scene automatically.

Pre-training scene change: room was selected first but default3viewdensefusion produced14points. One recorded min_num_pixels5to3 preprocessing repair produced55points, below100point integration threshold. Room stopped before any training/quality evaluation; no hidden negative metrics. Switched to fern from same existing archive to preserve default preprocessing and train settings. Fern uses original min_num_pixels5, not room repair. If fern also fails default preprocessing, stop this cohort and report; do not keep switching scenes or thresholds.
